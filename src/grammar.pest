number = @{("+" | "-")? ~ ('0'..'9')+}
operator = {"+" | "-" | "*" | "/" | "%" | "^" | "min" | "max"}
expr = {number | "(" ~ operator ~ expr+ ~ ")"}

expr_list = _{ SOI ~ operator ~ expr+ ~ EOI}

WHITESPACE = _{ (" " | NEWLINE | "\n" ) }
