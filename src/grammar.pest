number = @{("+" | "-")? ~ ('0'..'9')+}
symbol = {"+" | "-" | "*" | "/" | "%" | "^" | "min" | "max"}
sexpr = {"(" ~ expr* ~ ")"}
qexpr = {"{" ~ expr* ~ "}"}

expr = {number | symbol | sexpr | qexpr}

expr_list = _{ SOI ~ expr* ~ EOI}

WHITESPACE = _{ (" " | NEWLINE | "\n" ) }
