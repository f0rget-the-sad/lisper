number = @{("+" | "-")? ~ ('0'..'9')+}
symbol = {"+" | "-" | "*" | "/" | "%" | "^" | "min" | "max"}
expr = {number | symbol | sexpr}
sexpr = {"(" ~ expr* ~ ")"}

expr_list = _{ SOI ~ expr* ~ EOI}

WHITESPACE = _{ (" " | NEWLINE | "\n" ) }
